# 颐智云护·银龄智守 — API 接口测试文档

> **基地址**：`http://localhost:8080`  
> **后端框架**：Spring Boot 2.7 / Java 17  
> **端口**：8080  
> **数据库**：本地 MySQL（root）  
> **MQTT**：已运行消息监听器  

---

## 一、接口总览

| 序号 | 模块 | 方法 | 路径 | 说明 |
|------|------|------|------|------|
| 1 | 个人信息 | GET | `/api/profile` | 获取老人基本信息 |
| 2 | 个人信息 | PATCH | `/api/profile` | 更新老人基本信息 |
| 3 | 实时体征 | GET | `/api/vitals/latest` | 获取最新体征数据 |
| 4 | 体征趋势 | GET | `/api/vitals/history?period=week` | 获取体征历史趋势 |
| 5 | 告警列表 | GET | `/api/alerts` | 获取所有告警 |
| 6 | 最新告警 | GET | `/api/alerts/latest` | 获取最新一条告警 |
| 7 | AI 分析 | POST | `/api/ai/analyze` | 健康数据 AI 分析 |

---

## 二、接口详细说明（Apipost 可直接导入）

### 1️⃣ 获取个人信息

> **获取老人的姓名、年龄、性别、家属电话、住址**

- **方法**：`GET`
- **URL**：`http://localhost:8080/api/profile`

#### 请求示例（Apipost 直接请求即可，无需参数）

#### 响应示例（200 OK）
```json
{
  "name": "张大爷",
  "age": 78,
  "gender": "男",
  "familyPhone": "13800000000",
  "address": "未填写"
}
```

---

### 2️⃣ 更新个人信息

> **支持修改的字段：name / age / gender / familyPhone / address**

- **方法**：`PATCH`
- **URL**：`http://localhost:8080/api/profile`
- **请求头**：`Content-Type: application/json`

#### 请求体（JSON）
```json
{
  "field": "name",
  "value": "李大爷"
}
```

#### 字段说明

| field 可选值 | value 说明 | 校验规则 |
|-------------|-----------|---------|
| `name` | 姓名 | 2~20 字符 |
| `age` | 年龄 | 1~120 整数 |
| `gender` | 性别 | "男" 或 "女" |
| `familyPhone` | 家属电话 | 11 位有效手机号 |
| `address` | 住址 | 2~120 字符 |

#### 测试用例（Apipost 可逐条运行）

**测试 1：修改姓名**
- Request：`{"field": "name", "value": "李大爷"}`
- Expected：name 变为 "李大爷"

**测试 2：修改年龄**
- Request：`{"field": "age", "value": "80"}`
- Expected：age 变为 80

**测试 3：修改性别（错误值）**
- Request：`{"field": "gender", "value": "其他"}`
- Expected：400 Bad Request，`{"message":"性别请选择男或女"}`

**测试 4：修改手机号（格式错误）**
- Request：`{"field": "familyPhone", "value": "12345"}`
- Expected：400 Bad Request

**测试 5：修改住址**
- Request：`{"field": "address", "value": "6号楼 6-201"}`
- Expected：address 更新

---

### 3️⃣ 获取最新体征数据

> **返回老人的体温、心率、收缩压、舒张压、测量时间**  
> **⚠️ 注意：该接口数据为模拟生成，每次调用返回不同数值**

- **方法**：`GET`
- **URL**：`http://localhost:8080/api/vitals/latest`

#### 响应示例（200 OK）
```json
{
  "temperature": 36.5,
  "heartRate": 72,
  "systolic": 125,
  "diastolic": 82,
  "measuredAt": "2026-06-09T14:30:00Z"
}
```

#### 数值范围说明
| 字段 | 正常范围 | 说明 |
|------|---------|------|
| temperature | 36.0 ~ 37.0℃ | 模拟体温 |
| heartRate | 60 ~ 100 bpm | 模拟心率 |
| systolic | 90 ~ 140 mmHg | 模拟收缩压 |
| diastolic | 60 ~ 90 mmHg | 模拟舒张压 |

---

### 4️⃣ 获取体征历史趋势

> **返回一段时间内的体征变化趋势**

- **方法**：`GET`
- **URL**：`http://localhost:8080/api/vitals/history?period=week`

#### 查询参数

| 参数 | 类型 | 必填 | 可选值 | 默认值 | 说明 |
|------|------|------|--------|--------|------|
| period | string | 否 | `day` / `week` / `month` | `week` | 时间范围 |

#### 测试用例

**测试 1：过去 24 小时**
```
GET http://localhost:8080/api/vitals/history?period=day
```
- 返回 24 个数据点（每整点一个）

**测试 2：过去一周（默认）**
```
GET http://localhost:8080/api/vitals/history?period=week
```
- 返回 7 个数据点（每天一个）

**测试 3：过去一个月**
```
GET http://localhost:8080/api/vitals/history?period=month
```
- 返回 30 个数据点（每天一个）

#### 响应结构
```json
{
  "period": "week",
  "points": [
    {
      "label": "MM-dd 或 HH:00",
      "temperature": 36.5,
      "heartRate": 72,
      "systolic": 125,
      "diastolic": 82
    }
  ]
}
```

---

### 5️⃣ 获取告警列表

> **返回所有告警，按时间倒序排列**  
> **⚠️ 注意：当前为内置模拟数据，非数据库数据**

- **方法**：`GET`
- **URL**：`http://localhost:8080/api/alerts`

#### 响应示例（200 OK）
```json
[
  {
    "id": 1,
    "type": "HEART_RATE_HIGH",
    "detail": "122 bpm > 100",
    "occurredAt": "2026-05-03T08:20:00Z"
  },
  {
    "id": 2,
    "type": "TEMPERATURE_HIGH",
    "detail": "38.2°C > 37.5°C",
    "occurredAt": "2026-05-02T22:10:00Z"
  },
  {
    "id": 3,
    "type": "HEART_RATE_LOW",
    "detail": "48 bpm < 50",
    "occurredAt": "2026-05-01T06:30:00Z"
  }
]
```

#### AlertType 枚举说明
| 枚举值 | 说明 |
|--------|------|
| `HEART_RATE_HIGH` | 心率过高（>100 bpm） |
| `HEART_RATE_LOW` | 心率过低（<50 bpm） |
| `TEMPERATURE_HIGH` | 体温过高（>37.5℃） |
| `TEMPERATURE_LOW` | 体温过低 |
| `PRESSURE_HIGH` | 血压过高 |
| `PRESSURE_LOW` | 血压过低 |

---

### 6️⃣ 获取最新告警

> **返回最新的 1 条告警；若无告警则返回 204 No Content**

- **方法**：`GET`
- **URL**：`http://localhost:8080/api/alerts/latest`

#### 响应说明
- **有告警**：200 OK + 单条告警对象
- **无告警**：204 No Content（无响应体）

#### 响应示例（200 OK）
```json
{
  "id": 1,
  "type": "HEART_RATE_HIGH",
  "detail": "122 bpm > 100",
  "occurredAt": "2026-05-03T08:20:00Z"
}
```

---

### 7️⃣ AI 健康分析

> **对指定时间段内的体征数据进行智能分析，返回总结与建议**  
> **如果配置了 OPENAI_API_KEY，会调用 OpenAI 接口；否则使用内置规则引擎**

- **方法**：`POST`
- **URL**：`http://localhost:8080/api/ai/analyze`
- **请求头**：`Content-Type: application/json`

#### 请求体
```json
{
  "period": "week"
}
```

| 字段 | 类型 | 必填 | 可选值 | 说明 |
|------|------|------|--------|------|
| period | string | 是 | `day` / `week` / `month` | 分析的时间范围 |

#### 测试用例

**测试 1：日度分析**
```json
POST http://localhost:8080/api/ai/analyze
Body: {"period": "day"}
```

**测试 2：周度分析（推荐）**
```json
POST http://localhost:8080/api/ai/analyze
Body: {"period": "week"}
```

**测试 3：月度分析**
```json
POST http://localhost:8080/api/ai/analyze
Body: {"period": "month"}
```

**测试 4：无效周期（应返回 400）**
```json
POST http://localhost:8080/api/ai/analyze
Body: {"period": "year"}
```
- Expected：400 Bad Request，`{"message":"period: period 必须是 day、week 或 month"}`

#### 响应示例（200 OK）
```json
{
  "summary": "过去一周内，体温约36.3~36.7℃；心率约66~83 bpm；收缩压最高约128、舒张压最高约87 mmHg；有效点数7。 体温整体较平稳。",
  "suggestion": "建议保持规律作息与清淡饮食，适量饮水；如指标持续偏离个人基线，请咨询专业医生。",
  "fromLlm": false
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| summary | string | 健康数据总结 |
| suggestion | string | 健康建议 |
| fromLlm | boolean | `true`=来源于 OpenAI；`false`=来源于规则引擎 |

---

## 三、Apipost 使用指引

### 导入方式（推荐）

在 Apipost 中，按上述接口逐一创建请求即可。

### 配置说明

1. **新建接口集合** → 命名为「颐智云护 API」
2. **全局变量**：设置 `baseUrl = http://localhost:8080`
3. **公共请求头**：
   - `Content-Type: application/json`（仅 POST/PATCH 需要）
   - `Accept: application/json`

### 测试顺序建议

```
1. GET  /api/profile              → 查看初始数据
2. PATCH /api/profile             → 修改姓名/电话/地址
3. GET  /api/vitals/latest        → 查看当前体征
4. GET  /api/vitals/history       → 查看周趋势
5. POST /api/ai/analyze           → AI 分析
6. GET  /api/alerts               → 查看告警列表
7. GET  /api/alerts/latest        → 查看最新告警
```

---

## 四、项目架构说明

### 整体架构

```
┌─────────────────────────────────────────────────────┐
│                  前端（三端分离）                      │
│  ┌───────────┐  ┌─────────────┐  ┌────────────────┐ │
│  │ Flutter App│  │ Web 管理后台 │  │ 硬件端（MQTT）  │ │
│  │ (家属端)    │  │ (社区管理)   │  │ (传感器/手表)   │ │
│  └─────┬─────┘  └──────┬──────┘  └───────┬────────┘ │
└────────┼───────────────┼─────────────────┼───────────┘
         │ HTTP/JSON     │ HTTP/JSON      │ MQTT
         ▼               ▼                 ▼
┌─────────────────────────────────────────────────────┐
│              后端服务 (localhost:8080)                │
│  ┌────────────┐  ┌─────────────┐  ┌──────────────┐  │
│  │ Controller │→ │  Service    │→ │  Domain/DTO  │  │
│  │ (REST API) │  │ (业务逻辑)   │  │ (数据模型)    │  │
│  └────────────┘  └─────────────┘  └──────────────┘  │
│                    ↓  (内存存储/本地模拟)              │
│              当前无数据库依赖，数据在内存中               │
└─────────────────────────────────────────────────────┘
```

### 数据存储说明

| 数据类型 | 存储方式 | 说明 |
|---------|---------|------|
| 个人信息 | **内存** | UserProfile 对象，启动后一直在内存中，重启后重置 |
| 体征数据 | **内存** | VitalDataService 每次请求模拟生成新数据 |
| 告警数据 | **内存** | AlertService @PostConstruct 初始化 3 条模拟告警 |
| AI 分析 | **内存** | 基于 VitalDataService 的数据做规则分析 |

> **💡 提示**：当前后端未连接 MySQL 数据库，数据均存储在内存中。如需持久化，需要在 Service 层集成 MyBatis/MySQL。

---

## 五、常见问题

### Q1：后端如何启动？
```bash
cd backend
# 确保安装了 Java 17+ 和 Maven
mvn spring-boot:run
# 或
java -jar target/elderly-care-backend-1.0.0.jar
```

### Q2：如何配置 OpenAI 实现 LLM 分析？
编辑 `application.properties`：
```properties
app.ai.openai-key=sk-your-key-here
app.ai.openai-url=https://api.openai.com/v1/chat/completions
app.ai.model=gpt-4o-mini
```
配置后，`POST /api/ai/analyze` 返回的 `fromLlm` 字段将为 `true`。

### Q3：为什么重启后端后数据重置了？
因为当前使用内存存储。如需持久化，后续将集成 MySQL。

### Q4：前端 Flutter App 如何连接后端？
修改 `front/lib/config/api_config.dart` 中的 baseUrl 或在编译时指定：
```bash
flutter run --dart-define=API_BASE=http://192.168.x.x:8080
```

### Q5：Web 管理后台如何连接后端？
修改 `webAdmin/js/config.js`：
```javascript
window.APP_CONFIG = {
  USE_MOCK: false,        // false=连接后端
  API_BASE_URL: "http://localhost:8080"
};
```

---

## 六、错误码说明

| HTTP 状态码 | 说明 |
|------------|------|
| 200 | 请求成功 |
| 204 | 无内容（如无最新告警） |
| 400 | 请求参数错误（字段校验失败） |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

### 错误响应格式
```json
{
  "message": "具体的错误描述"
}
```
