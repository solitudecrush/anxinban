/**
 * 安心伴（Anxinban）智慧养老云平台的数据传输对象（DTO）包。
 *
 * <p>本包集中存放系统各层之间传递的数据载体，包括但不限于：</p>
 * <ul>
 *   <li>请求对象（Request）：接收前端或外部系统传入的参数，如登录、注册、重置密码等；</li>
 *   <li>响应对象（Response）：封装登录、业务查询等返回给调用方的数据；</li>
 *   <li>通用结果封装（{@link ApiResponse}、{@link PageResult}）：统一 API 返回格式及分页结果；</li>
 *   <li>业务 DTO：老人、设备、告警、工单、健康记录、SOS、通知等各个领域的数据视图。</li>
 * </ul>
 *
 * <p>设计原则：DTO 仅负责数据封装，不包含业务逻辑；字段类型与结构由调用方/服务层约定，
 * 时间字段统一使用字符串格式（如 ISO-8601 或 yyyy-MM-dd HH:mm:ss），便于前后端交互。</p>
 */
package com.anxinban.dto;
